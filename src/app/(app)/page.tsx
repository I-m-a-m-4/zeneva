// This page is intentionally left blank to resolve a build conflict
// with the main marketing page at /app/page.tsx.
// The root of the authenticated app should be /dashboard.
export default function AppRootPage() {
  return null;
}
